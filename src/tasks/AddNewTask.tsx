import {View, Text} from 'react-native';
import React from 'react';

const AddNewTask = () => {
  return (
    <View style={{flex: 1}}>
      <Text style={{flex: 1, fontSize: 20, marginTop: 50}}>AddNewTask</Text>
    </View>
  );
};

export default AddNewTask;
